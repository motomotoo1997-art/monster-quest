name: Gold Rush Godot CI

on:
  push:
    branches:
      - feature/gold-rush-vertical-slice
    paths:
      - 'gold-rush-roguelike/**'
      - '.github/workflows/gold-rush-godot-ci.yml'
  workflow_dispatch:

jobs:
  godot-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Godot 4.7.2
        shell: bash
        run: |
          set -euo pipefail
          wget -q https://github.com/godotengine/godot/releases/download/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip -O /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          sudo ln -s /tmp/godot/Godot_v4.7.2-stable_linux.x86_64 /usr/local/bin/godot
          godot --version

      - name: Import project headlessly
        run: godot --headless --path gold-rush-roguelike --editor --quit

      - name: Main scene smoke test
        run: godot --headless --path gold-rush-roguelike --script res://tests/smoke_main_scene.gd
